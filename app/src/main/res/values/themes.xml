<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.JobMatch" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">#5B7BFF</item>
    </style>

    <!--
      Tema aplicado a MainActivity en el Manifest.
      postSplashScreenTheme indica al compat library qué tema restaurar
      una vez que el splash termina y Compose está listo.
    -->
    <style name="Theme.JobMatch.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@drawable/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.JobMatch</item>
    </style>

</resources>
